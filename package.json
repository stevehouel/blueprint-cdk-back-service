{
  "name": "root",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "watch": "lerna run --parallel --no-sort watch",
    "test": "CI=true lerna run --parallel --no-sort test",
    "synth": "CI=true lerna run --parallel --no-sort synth",
    "test:watch": "lerna run --parallel --no-sort test:watch",
    "lint": "lerna run --parallel --no-sort lint",
    "lint-fix": "lerna run --parallel --no-sort lint-fix",
    "test-functional": "lerna run test-functional",
    "create-test-user": "lerna run create-test-user"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@typescript-eslint/eslint-plugin": "4.22.0",
    "@typescript-eslint/parser": "~4.22.0",
    "babel-eslint": "^10.1.0",
    "eslint": "~7.25.0",
    "jest": "^26.6.3",
    "lerna": "^4.0.0",
    "ts-jest": "~26.5.5"
  },
  "dependencies": {}
}
